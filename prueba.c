#include <stdio.h>
#include <stdlib.h>
#include "vectorDinamico.h"


int main(){
	Vector v1;
	allocateMemory(v1, 16);
/*
	for (int i = 0; i < 10; ++i)
	{
		push(vector,i+1);
		printf("%d, ", at(vector,i));
	}
*/

	return 0;
}